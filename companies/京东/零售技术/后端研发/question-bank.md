# 京东｜零售技术｜后端研发｜题库

## 岗位方向
- 后端开发
- 服务端研发
- 零售交易 / 商品 / 库存 / 履约链路后端

## 高频题
- MySQL 覆盖索引、回表、最左前缀分别是什么？
- 为什么会出现慢查询？如何结合执行计划定位问题？
- Redis 在商品详情、库存、会场页等场景中通常怎么用？
- 缓存一致性问题怎么处理？延迟双删适合什么情况？
- 分布式锁常见实现有哪些？会踩哪些坑？
- MQ 的重复消费、消息丢失、顺序消费怎么处理？
- Java 线程池参数如何根据业务峰值配置？
- synchronized 和 ReentrantLock 的区别是什么？
- Spring 事务失效的常见场景有哪些？
- 什么是幂等？为什么订单、支付、退款特别需要幂等？
- 如何做接口限流、防刷、降级？
- 分库分表后跨库查询和分页怎么处理？

## 并发与JVM
- HashMap扩容时如何让原数据均匀分布？(e.hash & oldCap)==0的原理？
- 为什么Redis是单线程的却很快？IO多路复用机制（select/poll/epoll）区别？
- 如何对10亿个数据进行排序？外部排序+多路归并的具体流程？
- Cglib和JDK动态代理的区别？SpringBoot 2.x默认用哪个？为什么？
- 类加载过程五步？双亲委派模型的好处？什么场景需要打破？
- volatile关键字的作用？为什么双重检查锁单例要用volatile？
- servlet的生命周期？init、service、destroy分别在什么时候调用？

## 设计模式与场景
- 多项审批流程用什么设计模式？责任链模式如何实现？
- 观察者模式和责任链模式的区别？各自适合什么场景？
- 如何设计一个限流算法？固定窗口、滑动窗口、漏桶、令牌桶的优缺点？

## 算法题
- 两数之和 / 三数之和
- 反转链表 / K 个一组反转链表
- LRU 缓存实现
- 二叉树最近公共祖先
- Top K 问题
- 最长无重复子串
- 合并区间
- 二分查找边界问题

## 项目题
- 讲一个你参与过的高并发交易或库存项目。
- 如果大促流量暴涨，如何评估容量并提前做保护？
- 库存扣减为什么容易出问题？你会怎么治理超卖、少卖和回滚？
- 订单、库存、支付、履约之间的一致性如何保证？
- 商品详情页接口为什么会慢？你做过哪些优化？
- 如何做线上故障排查和复盘？
- 如果消息堆积导致履约链路延迟，你会怎么止损？
- 讲一次真正量化过收益的优化案例。

## 系统设计题
- 设计一个高并发库存中心。
- 设计一个秒杀下单系统。
- 设计一个订单状态流转系统。
- 设计一个商品详情缓存架构。
- 设计一个大促限流与降级系统。
- 设计一个异步补偿与对账系统。
- 设计一个退款回调幂等处理链路。

## 零售交易 / 库存 / 履约专题

> 来源：综合牛客网2024-2025京东零售后端面经、京东JDY一面面经、京东云开发者社区零售技术年度总结，提取零售核心链路高频场景题。

### 1. 库存扣减与超卖治理
- 库存预扣、实扣、回补三阶段分别在什么时机触发？如果预扣成功但下单失败，回补延迟会导致什么问题？
- Redis Lua 原子扣减库存的实现思路？为什么不能用先 GET 再 DECR 的两步操作？
- 乐观锁扣库存（version CAS）和悲观锁扣库存（SELECT FOR UPDATE）各自适合什么场景？京东零售大促时倾向用哪种？
- 热点商品库存被打爆时，如何用分桶库存（库存分片）降低单 key 热点？分桶后库存归集和展示一致性怎么处理？
- 库存扣减和订单创建不在同一个数据库，如何保证分布式事务一致性？TCC 和最终一致性方案各自的优缺点？

### 2. 订单状态流转与一致性
- 订单从创建→待支付→已支付→待发货→已发货→已完成→售后，状态机怎么设计？如何防止非法状态跳转？
- 订单超时未支付自动关闭用什么方案？延迟消息、定时任务扫表、时间轮各自的优缺点？
- 支付回调幂等怎么做？如果支付系统重复回调三次，订单系统如何保证只处理一次？
- 订单、库存、优惠券三个服务的状态不一致时（比如订单成功但库存回补失败），如何用对账系统兜底？对账的频率和粒度怎么定？

### 3. 履约链路与物流协同
- 京东零售超过 90% 自营订单 24 小时内履约，背后的仓网协同和库存调拨算法核心思路是什么？
- 订单拆单的触发条件有哪些？（不同仓库、不同品类、不同配送方式）拆单后如何保证用户看到的是统一订单视图？
- 履约链路中，如果某个仓库缺货需要调拨，调拨延迟导致 ETA（预计送达时间）不准，系统如何动态修正 ETA 并通知用户？
- 逆向履约（退货退款）和正向履约共用库存池时，如何防止退货回补的库存被立即抢走导致账面库存虚高？

### 4. 商品详情与缓存架构
- 商品详情页接口为什么容易慢？涉及哪些下游服务（商品基础信息、价格、促销、库存、评价）？如何用多级缓存（本地缓存 + Redis + CDN）优化？
- 大促会场页热点商品缓存击穿怎么处理？互斥锁重建 vs 逻辑过期 vs 永不过期+异步刷新，各自适合什么场景？
- 商品价格在促销期间频繁变动，缓存和数据库的价格不一致会导致什么业务问题？如何用版本号或消息驱动保证价格缓存的最终一致性？

### 5. 大促容量与稳定性
- 大促前如何做容量评估？从历史流量、业务增长预期、压测数据三个维度怎么推算需要多少机器？
- 全链路压测怎么做？影子库、影子表、流量标记染色的原理？如何保证压测流量不污染线上数据？
- 大促期间核心链路和非核心链路的降级策略怎么定？哪些功能可以降级（推荐、评价、积分），哪些绝不能降级（下单、支付、库存）？
- 如果大促期间某个下游服务（比如优惠券服务）响应变慢，如何用熔断+降级保护主链路？Sentinel 和 Hystrix 的核心区别？

## 面经真题补充：中间件与项目深挖专项

> 来源：牛客网2024京东零售后端开发校招三面面经，提取项目深挖+中间件+设计模式高频追问。

### 1. MQ 项目实战与失败补偿
- 项目中用 MQ 做了什么事情？为什么选择异步而不是同步调用？
- 如果异步 MQ 发奖失败了怎么办？完整的补偿链路怎么设计？（消费者重试→死信队列→本地消息表+定时补偿→人工兜底）
- MQ 消费者幂等怎么做？如果同一条消息被投递三次，业务侧如何保证只处理一次？

### 2. Redis 分布式锁与热 key 深挖
- 分布式锁 setNX 的底层原理是什么？为什么 Redis 单线程能保证原子性？
- 分布式锁过期时间设多长？如果业务没执行完锁就过期了怎么办？（看门狗续期机制）
- 访问量特别大时形成热 key 会有什么风险？京东 JD-hotkey 的实时探测思路？
- 热 key 拆分（key 加随机后缀分散到多个 slot）后，读取时如何聚合结果？

### 3. RabbitMQ 原理与对比
- RabbitMQ 的核心模型？Exchange 的四种类型分别适合什么场景？
- RabbitMQ 的消息持久化需要哪三层都设置 durable？
- RabbitMQ 和 RocketMQ、Kafka 各自适合什么业务场景？

### 4. Spring 事务嵌套与动态代理
- Spring 事务传播行为有哪几种？REQUIRED、REQUIRES_NEW、NESTED 的区别？
- Spring 事务嵌套时，内层事务回滚会影响外层吗？（取决于传播行为）
- Cglib 和 JDK 动态代理的区别？SpringBoot 2.x 为什么默认改用 Cglib？

### 5. 追问链路
- 项目拷打 → MQ 使用场景 → 失败补偿 → 幂等设计（一面典型追问链路）
- Redis 高性能 → IO 多路复用 → epoll 原理 → 热 key 治理（一面 Redis 追问链路）
- 限流算法设计 → 分布式限流 → Sentinel vs 自研（二面场景追问链路）

## 网络协议与序列化专题（2025春招面经补充）

> 来源：牛客网2025京东零售产研后端开发春招三面面经（已OC），提取网络协议和序列化方向高频追问。

### 1. 序列化协议选型
- protobuf和JSON各自适合什么场景？为什么内部RPC服务间通信优先用protobuf？
- protobuf为什么速度快体积小？varint编码、tag+wire_type标识、二进制编码各自的作用？
- protobuf的IDL版本兼容机制？新增字段、删除字段、修改字段类型分别会怎样？
- 除了protobuf和JSON，还有哪些序列化方案？（Thrift、Avro、MessagePack、Kryo）各自的优缺点？

### 2. TCP协议深挖
- TCP粘包拆包的原因？为什么UDP没有粘包问题？
- 解决TCP粘包的四种方案？（固定长度、分隔符、长度前缀、应用层协议边界）各自的优缺点？
- protobuf配合TCP传输时，通常用什么方式处理消息边界？（长度前缀：4字节length + protobuf payload）
- TCP Nagle算法和延迟ACK的交互会导致什么问题？如何用TCP_NODELAY优化？

### 3. 分布式协调与服务发现
- ZooKeeper的核心数据模型？ZNode的类型（持久/临时/顺序）分别用于什么场景？
- ZooKeeper做分布式锁的实现原理？临时顺序节点+Watch机制的流程？
- ZooKeeper、etcd、Nacos作为注册中心的对比？CP vs AP模型的选择依据？
- 京东内部的JSF服务框架和开源Dubbo的异同？

### 4. 集合框架与面向接口编程
- 为什么声明类型用List而不是ArrayList？面向接口编程的好处？
- List的常见实现类有哪些？ArrayList、LinkedList、CopyOnWriteArrayList各自的适用场景？
- Collections.unmodifiableList和List.of()创建的不可变列表有什么区别？
- Stream API中collect(Collectors.toList())返回的是什么类型？能保证是ArrayList吗？

### 5. 追问链路
- protobuf → 为什么快 → 编码原理 → IDL兼容 → gRPC流式通信
- TCP粘包 → 长度前缀 → 自定义协议设计 → Netty的LengthFieldBasedFrameDecoder
- ZooKeeper → 临时节点 → 分布式锁 → 羊群效应 → Curator框架

## 反问方向
- 这个岗位主要负责商品、交易、库存还是履约侧链路？
- 团队当前最关注的核心稳定性指标是什么？
- 大促前后的压测、扩容、预案演练一般怎么做？
- 这个团队更偏业务快速迭代还是平台化建设？
- 新人通常多久能独立负责线上模块？


## 清洗版补充：零售交易、库存与履约统一治理框架

> 说明：这一节把京东零售后端里最容易被分散追问的“下单、锁库、支付、拆单、履约、退款、补货、回放补偿”问题，进一步收敛成一套统一治理框架。重点不再只是回答某个接口怎么做，而是先回答：**谁持有库存真相、谁驱动订单主状态、履约失败后哪些能回滚、哪些只能补偿。**

### 1. 订单主状态与库存真相来源
- 零售交易链路里同时存在商品可售库存、预占库存、支付确认库存、履约可发库存时，系统如何定义唯一库存真相，避免商品、交易、仓储各算一套？
- 订单创建、锁库、支付回调和拆单异步推进时，谁来决定订单主状态，怎样避免“库存已扣但订单未成”或“订单已成但仓储无货”？

### 2. 大促峰值下的交易承接与库存保护
- 会场流量、搜索流量、推荐流量同时灌入同一商品时，后端如何在接入层、交易层、库存层、履约层做分层限流，保证先保什么、后保什么？
- 当热点商品库存极少但曝光极大时，系统怎样让前台展示、库存扣减和下单资格尽量同口径，减少“能看不能买”的撕裂体验？

### 3. 支付、履约与逆向退款收敛
- 支付成功但仓储履约失败、部分商品缺货、拆单后部分包裹无法发出时，订单状态、库存回补、用户承诺和退款路径应该如何统一收口？
- 为什么零售场景里很多问题不能简单靠“订单回滚”解决，而必须区分交易回退、库存回补、履约补偿和用户通知四条线分别处理？

### 4. 异步消息、补数回放与恢复期治理
- 如果 MQ 堆积、支付回调重试、库存补偿任务和履约补数同时发生，系统如何设计事件版本、幂等键和事实重建顺序，避免越修越乱？
- 对于“已退款又被重放发货”“已回补库存又被二次扣减”“订单终态已关闭但履约侧迟到更新”这类恢复期问题，怎样防止二次伤害？

### 5. 平台化抽象与多业务线复用
- 京东零售技术为什么不能只围绕单一订单链路写死规则，而要抽象成可复用的交易、库存、履约治理框架去支撑主站、秒杀、促销、逆向等多场景？
- 哪些能力适合沉到平台层（状态机、幂等、补偿、对账、审计），哪些必须留给业务线按促销规则或履约策略定制？

### 6. 面试回答建议
- 回答京东零售后端题时，优先围绕：**订单主状态、库存真相、峰值保护、履约收口、恢复期治理** 5 条主线展开。
- 少把交易、库存、履约拆成三个孤立模块去答，要讲清楚：**零售后端真正难的是高峰期里多个系统同时改写事实时，怎样仍然只保留一个可执行主状态。**
